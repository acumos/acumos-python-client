{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "self": {
        "name": "acumos.schema.model.documentation",
        "version": "0.1.0"
        },
    "properties": {
        "schema": {"type": "string"},
        "resources": {
          "type": "object",
          "properties": {
            "introduction": {"$ref": "#/definitions/Resource"},
            "background": {"$ref": "#/definitions/Resource"},
            "synopsis": {"$ref": "#/definitions/Resource"},
            "example": {"$ref": "#/definitions/Resource"}
            },
          "additionalProperties": false
          }
        },
    "required": ["resources", "schema", "name"],
    "definitions": {
        "Resource": {
            "type": "object",
            "properties": {
                "encoding": {"enum": ["markdown", "html", "text"]},
                "body": {"type": "string"}
                },
            "required": ["encoding", "body"],
            "additionalProperties": false
            }
        }
}
